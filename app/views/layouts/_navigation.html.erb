<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">User Management</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to 'Dashboard', root_path, class: 'nav-link' %>
        </li>

        <% if current_user.admin? %>
          <li class="nav-item">
            <%= link_to 'Manage Users', users_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Manage Permissions', permissions_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Manage Circles', circles_path, class: 'nav-link' %>
          </li>
        <% end %>

        <% if current_user.admin? || current_user.circle_admin? %>
          <li class="nav-item">
            <%= link_to 'Manage Branches', branches_path, class: 'nav-link' %>
          </li>
        <% end %>

        <li class="nav-item">
          <%= link_to 'Manage Records', records_path, class: 'nav-link' %>
        </li>
      </ul>

      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
            <%= current_user.full_name %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><span class="dropdown-item-text"><%= current_user.role.titleize %></span></li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <%= button_to 'Log Out', logout_path, method: :delete, class: 'dropdown-item' %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
